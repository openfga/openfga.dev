export enum Keywords {
  NAMESPACE = 'type',
  RELATIONS = 'relations',
  SELF = 'self',
  DEFINE = 'define',
  AS = 'as',
  OR = 'or',
  AND = 'and',
  FROM = 'from',
  BUT_NOT = 'but not',
  COMMENT = 'comment',
}
